# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# mkcert-generated local dev cert/key + the CA tool binary itself — both are
# machine-specific security artifacts (tied to this laptop's trusted local
# CA and static LAN IP), never to be committed. See vite.config.ts.
*.pem
mkcert.exe
