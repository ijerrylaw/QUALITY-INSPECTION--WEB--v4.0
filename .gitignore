node_modules
dist
.env
.DS_Store

# Vitest browser-mode auto-captures a screenshot on any failing test
__screenshots__/
.vitest-attachments/

# Real One Glove Group production data exports (monthly) — must never enter git history
*.xlsx
*.xls

# Local editor / assistant workspace state (launch configs, permission
# allowlists, per-tool caches). Machine-local and never part of a release —
# these are also excluded from the installer package by install/package.ps1.
.claude/
.cursor/
.windsurf/
.aider*
.continue/
.idea/
.vscode/
